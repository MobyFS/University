#МОДУЛЬ2
from math import*
def func (x,y):
        max = sin(x)
        if cos(y)>max:
            max=cos(y)
        if tan(x*y)>max:
            max=tan(x*y)
        p=max
        print("\nРезультат = ",format(p))
